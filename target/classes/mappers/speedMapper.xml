<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.bill.mapper.speedMapper">
    
    <select id="getSpeed" resultType="com.bill.vo.speed.speedVO">
        SELECT time,Speed,route FROM concat WHERE Speed NOT IN('')
    </select>
 
 
    
</mapper>